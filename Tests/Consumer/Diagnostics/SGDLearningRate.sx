use Tensor
use Tensor.NN
use Tensor.Optim

func main() {
    var values:float[] = [1.0]
    var parameters:NN.Parameter[] = [NN.Parameter("weight", Tensor.vector(values))]
    var optimizer = Optim.SGD(parameters, learning_rate:0.0)
    optimizer.step()
}
