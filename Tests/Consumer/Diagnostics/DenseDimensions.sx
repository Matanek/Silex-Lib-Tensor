use Tensor
use Tensor.Neural

func main() {
    var values:float[] = [1.0, 2.0, 3.0]
    var dense = Neural.Dense("dense", 2, 1, 7)
    let output = dense.forward(Tensor(values, [1, 3]))
    print(output.count())
}
