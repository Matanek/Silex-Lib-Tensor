use Tensor
use Tensor.NN

func main() {
    var values:float[] = [1.0, 2.0, 3.0]
    var dense = NN.Dense("dense", 2, 1, 7)
    let output = dense.forward(Tensor(values, [1, 3]))
    print(output.count())
}
