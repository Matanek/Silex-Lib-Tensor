use GFX.GPU
use Tensor
func main() { var device = GPU.Device(); var values:int32[] = [1 as int32, 2 as int32]; let left = Tensor.vector(values).to(device); let result = left.dot(Tensor.vector(values).to(device)); print(result.count()) }
