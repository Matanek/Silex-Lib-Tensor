use GFX.GPU
use Tensor
func main() { var device = GPU.Device(); var values:int32[] = [1 as int32]; let left = Tensor(values, [1, 1]).to(device); let result = left.matmul(Tensor(values, [1, 1]).to(device)); print(result.count()) }
