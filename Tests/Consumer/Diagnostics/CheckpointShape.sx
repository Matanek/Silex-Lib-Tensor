use Tensor.Neural

func main() {
    var layers:Neural.Layer[] = [Neural.Dense("dense", 2, 1, 7)]
    var model = Neural.Sequential(layers)
    Neural.Checkpoint.load(model, "Fixtures/CheckpointShape.sxtc")
}
